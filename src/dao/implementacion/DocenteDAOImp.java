/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

package dao.implementacion;

import dao.DocenteDAO;


/** @author IgnacioSaravia <ILSSoftware2014>  */
public class DocenteDAOImp implements DocenteDAO {
    /*public List<Docente> seleccionarTodo() {
        
        * Session session = HibernateUtil.getSessionFactory().openSession();
        session.beginTransaction();
        /* se borra session.save(miDocente)
        /* se define criteria, se agrega list 
        Criteria criteria= session.createCriteria(Docente.class);
        
        * List <Alumno> listadoDocente = criteria.list();
        session.close();
        return(listadoDocente);
        /* se devuelve con return(list) porque es un metodo public de tipo list 
    }*/
    

}
